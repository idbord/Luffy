<% if (post.tags && post.tags.length){ %>
	<i class="fa fa-bookmark"></i>
	<div class="article-tag tagcloud">
		<%- list_tags(post.tags, {
		    show_count: false,
		    class: 'article-tag'
		  }) %>
	</div>
<% } %>